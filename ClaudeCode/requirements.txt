# Python dependencies for Unity Command Server client
# Install: pip install -r requirements.txt

websockets>=12.0
pywin32>=306

# Note: This script is Windows-only
# pywin32 is used for window management (activating Unity window)
